/**
 * Attempts to parse object as JSON.
 * Returns original object if JSON parse failed (silently).
 */
export declare function _jsonParseIfPossible(obj: any, reviver?: (this: any, key: string, value: any) => any): any;
